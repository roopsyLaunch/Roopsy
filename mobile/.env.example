# Copy to ".env" in this folder and set your PC's LAN IP (same Wi‑Fi as phone).
# Windows: run `ipconfig` → IPv4 Address (e.g. 192.168.1.42)
# Do NOT use localhost on a real phone — it points at the phone itself.
EXPO_PUBLIC_API_URL=http://10.139.145.5:5000
